# CLAUDE.md

`AGENTS.md` is the single source of truth for coding-agent guidance in this repo. It is imported below so Claude Code and every other agent share one canonical file.

@AGENTS.md

Claude-specific rules (if any) can go beneath this import; shared guidance belongs in `AGENTS.md`.
